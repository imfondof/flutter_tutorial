import 'package:flutter/material.dart';

class Index extends StatelessWidget {
  const Index({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Evimed"),
      ),
      body: Center(
        child: Column(
          children: <Widget>[
            const Text("fdsafdsf"),
            const Text("fdsafdsf"),
            const Text("fdsafdsf"),
            const Text("fdsafdsf")
          ],
        ),
      ),
    );
  }
}
